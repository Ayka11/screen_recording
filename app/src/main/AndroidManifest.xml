<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright (c) ScreenCastPro 2017.
  -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.openapps.screenrecorderpro">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Deprecated in Android 12+; use MANAGE_EXTERNAL_STORAGE for broad access -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="com.openapps.screenrecorderpro.TOGGLETOUCHES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:theme">
        <activity
            android:name="com.openapps.screenrecorderpro.MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcut" />
        </activity>

        <service
            android:name="com.openapps.screenrecorderpro.RecorderService"
            android:exported="false" />
        <service
            android:name="com.openapps.screenrecorderpro.FloatingControlService"
            android:exported="false" />

        <service android:name="org.openudid.OpenUDID_service">
            <intent-filter>
                <action android:name="org.openudid.GETUDID" />
            </intent-filter>
        </service>

        <activity
            android:name="com.openapps.screenrecorderpro.AboutActivity"
            android:label="@string/about_activity_label" />

        <!-- TODO: For future Firebase dynamic linking -->
        <activity android:name="com.openapps.screenrecorderpro.DeepLinkActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="screenrecording"
                    android:scheme="od" />
                <data
                    android:host="screenrecord.orpheusdroid.com"
                    android:scheme="https" />
                <data
                    android:host="screenrecord.orpheusdroid.com"
                    android:scheme="http" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity android:name="com.openapps.screenrecorderpro.PrivacyPolicy"
            android:label="Privacy Policy" />
    </application>

</manifest>